const config = {
    port : 3000,
    database: {
        DATABASE: 'nodesql',
        USENAME:'root',
        PASSWORD:'Shu520...',
        PORT: '3306',
        HOST: 'localhost'
    }
}

module.exports = config